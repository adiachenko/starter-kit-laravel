{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "format": "prettier --write . --list-different"
    },
    "devDependencies": {
        "prettier": "3.8.0"
    }
}
